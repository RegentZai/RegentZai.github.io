name: hello
on:
  push:
    branches:
      - dev
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: check
      uses: actions/checkout@v3
    - name: hello
      run: chmod u+x hello.sh && ./hello.sh
    - name: merge
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      run: |
        git config --global user.name "RegentZai"
        git config --global user.email "58322558@qq.com"
        git config --global --unset https.proxy
        git config --global --unset http.proxy
        git fetch
        git checkout dev
        git checkout -b main2
        git checkout main
        git merge main2 ----allow-unrelated-histories
        git commit
        git branch -D main2
